{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "uv-sync",
      "type": "shell",
      "command": "uv",
      "args": [
        "sync",
        "--all-packages"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
